{
  "scripts": {
    "deploy-devnet": "anchor build && anchor deploy --provider.cluster devnet --provider.wallet ~/config/solana/nuked.json",
    "deploy-mainnet": "anchor build && anchor deploy --provider.cluster mainnet --provider.wallet ~/config/solana/nuked.json"
  },
  "dependencies": {
    "@project-serum/anchor": "^0.18.2",
    "@solana/spl-token": "^0.1.8"
  },
  "devDependencies": {
    "@types/chai": "^4.3.0",
    "@types/jest": "^27.4.0",
    "@types/mocha": "^9.1.0",
    "chai": "^4.3.4",
    "mocha": "^9.0.3",
    "ts-mocha": "^8.0.0",
    "typescript": "^4.3.5"
  }
}
