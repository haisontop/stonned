export interface Roadmap {
  id: string
  title: string
}

export interface RoadmapResponse {
  id: string
  name: string
}

export interface Vision {
  id: string;
  title: string;
  subtitle: string;
  imageURL?: string;
  description?: string;
}